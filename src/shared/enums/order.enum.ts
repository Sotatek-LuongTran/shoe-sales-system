export enum OrderStatusEnum {
    PENDING = 'pending',
    SHIPPED = 'shipped',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    EXPIRED = 'expired',
}

export enum OrderPaymentStatusEnum {
    PAID = 'paid',
    UNPAID = 'unpaid'
}